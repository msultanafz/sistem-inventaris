import './bootstrap'; // Ini biasanya ada secara default

import Alpine from 'alpinejs'; // Baris ini mengimpor Alpine

window.Alpine = Alpine; // Baris ini membuat Alpine tersedia secara global

Alpine.start(); // Baris ini memulai Alpine.js